# Domyślne reguły ProGuard/R8.
# W tym projekcie isMinifyEnabled = false, więc ten plik nie jest aktywnie
# używany — zostaje na wypadek włączenia skracania/obfuskacji kodu później.

-keepattributes Signature
-keepattributes *Annotation*
